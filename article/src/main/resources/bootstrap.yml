server:
  port: 8001
spring:
  application:
    name: article
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.88.130:8848
      config:
        server-addr: 192.168.88.130:8848
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: jdbc.yaml # 共享mybatis配置

